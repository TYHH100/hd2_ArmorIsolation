# CM-14 隔离候选记录

后续状态：用户已自行测试首版，体甲生效但头盔遗漏。当前交付目录已更新为 51 资源的体甲 + 头盔候选，见 [头盔修正记录](cm14-helmet-fix.md)。下文保留首版实现与当时的验证边界，不代表当前修正版统计。

2026-09-19，用户授权使用 `ALLENES替换技术兵和医疗套/CM-14`，通过补丁内容修改与 ReShade 插件测试消除连带覆盖。构建完成后用户选择“先保留测试文件，稍后测试”，本轮未安装或改写游戏。

交付目录：[dist/cm14-isolated](../dist/cm14-isolated/README.md)。

## 实现

- 原输入：`9ba626afa44a3aa3.patch_200` 及两路配套文件，保留当前已修复版本；未使用 ZIP 或历史备份替换输入。
- 目标：CM-14，Kit `38aa207d`，Archive `b0db7f4f0a11debd`。
- 源补丁：36 Unit、1 Material、11 Texture；26 Unit属于CM-14，另10项剔除。
- 输出：38个私有资源，三文件合计200,440,720字节。资源映射包含目标Kit身份，私有ID同时在已检查范围核对完整64位与高32位碰撞。
- 51个定点引用改写：40个Unit→Material、11个Material→Texture。32位槽名、用途哈希、骨骼及网格索引未改。
- 12个外部材质/基础材质检查闭合，没有引用本补丁修改的11个纹理。保留这些未修改依赖，避免不必要复制。
- 插件用匹配本机ReShade6.5.1的API17；版本、代码和结构守卫通过，且全部新资源ready后，发布CM-14描述副本。唯一游戏所有的写入为Kit数组的一次8字节指针CAS。
- 私有配置分配由进程持有至退出，最多16次发布；不做运行中恢复、不调用未验证的引擎函数。

## 验证

- 构建器4个测试通过，原生候选生成和原子发布测试通过。
- 原输入三文件SHA前后保持一致；输出TOC不含源48项中的任何旧资源ID。
- GPU/stream有效数据逐项一致；三路边界、条目索引、256字节内存缓冲计算和独立Unit解析通过。
- 只读同代码探针：[cm14-before-install-probe.txt](cm14-before-install-probe.txt)，安装前私有资源0/38、目标原引用26、私有引用0、其他Kit私有引用0。
- 游戏内加载、配置发布及视觉验证均未做。不能标注“已消除污染”。

安装与恢复由 [manage_cm14_test.ps1](../tools/manage_cm14_test.ps1) 管理，拒绝游戏运行中操作、旧ID覆盖冲突、非连续补丁编号以及卸载时文件身份变化。

源码：[补丁构建器](../tools/build_cm14_isolated.py)、[ReShade插件](../src/cm14_isolation_addon.cpp)、[只读资源就绪探针](../include/cm14_resource_probe.hpp)。
