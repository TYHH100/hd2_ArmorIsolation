这套工具的核心是：**给模组资源分配独立 ID，并让指定装备在游戏运行时引用这些独立资源，从而减少共享资源造成的连带替换。**

它由两个部分共同完成：

- **Python 生成工具**：分析模组、处理资源引用、重新打包，并生成配置。
- **C++ ReShade 插件**：在游戏运行时检查配置与资源，将目标装备的资源引用切换到隔离版本。

以下说明依据当前仓库实现。只读取代码，没有修改文件或推送。

**1. 为什么普通替换模组会影响其他装备**

游戏里的“装备”和“模型资源”不是一一对应关系。

一件装备通过自己的配置引用模型，模型再引用材质和纹理。不同装备可能共用其中某些资源。

例如：

```text
装备 A ─┐
        ├→ 模型 U → 材质 M → 纹理 T
装备 B ─┘
```

普通替换模组沿用原资源 ID，将模型 U 的内容换成模组模型。游戏查询 U 时得到替换后的内容，A、B 就可能一起变化。

即使给补丁文件换名字，只要内部资源仍使用 U 的 ID，这种共享关系就没有解除。

隔离工具希望生成下面的关系：

```text
装备 A → 私有模型 U_A → 私有材质 M_P → 私有纹理 T_P
装备 B → 原模型 U   → 原材质 M     → 原纹理 T
```

这里同时需要完成两件事：

1. 让私有资源确实存在于补丁中。
2. 让装备 A 改为引用私有资源。

只有重打包而没有运行时引用修改，装备仍会寻找原 ID；只有引用修改而没有正确加载私有资源，则可能缺模型或缺材质。

**2. 工具如何描述一件装备**

工具使用绑定游戏版本的装备快照，描述装备的层级关系：

```text
Kit：一件装备的配置
 └─ Body：不同体型对应的配置
     └─ Piece：该体型下的外观部件
         ├─ Slot：所在部位
         ├─ Unit：模型资源
         ├─ 纹理等资源引用
         └─ Type、Weight 等元数据
```

几个概念需要区分：

| 概念 | 在这里的作用 |
|---|---|
| Kit ID | 确定是哪件装备 |
| Unit ID | 确定使用哪个模型资源 |
| Material ID | 确定模型使用的材质 |
| Texture ID | 确定材质或部件使用的纹理 |
| Slot | 确定部件位置 |
| Body | 区分体型配置 |

同名体甲与头盔是不同 Kit，必须分别处理。

当前部位约定是 Slot0 头盔、Slot1 披风、Slot2 躯干、Slot3 髋部。**隔离流程保留默认披风配置，但不对披风执行隔离和依赖等待。**

工具也不会因为资源共享，就假定两件装备的被动、属性或其他配置相同。

**3. 第一步：核对版本和输入**

生成前会核对本机游戏 DLL 和装备快照。

原因在于工具必须理解：

- 装备数据的结构；
- 模型和材质内部引用的位置；
- 运行时装备表的布局；
- 哪些字段允许修改。

这些信息与游戏版本相关。版本不同，旧结构可能不再成立。

因此，当前实现检查的是绑定版本及哈希，并不是读取一个版本号后就允许任意游戏数据继续处理。

输入则分两条路径：

| 输入 | 处理方式 |
|---|---|
| 单个主补丁及配套数据 | 通用单补丁生成器 |
| 带 manifest 的模组目录 | 模块化生成器，先解析选项结构 |

补丁通常有三路数据：

```text
主补丁 .patch_N
配套数据 .patch_N.stream
GPU 数据 .patch_N.gpu_resources
```

主文件包含资源目录等信息。每条资源记录描述资源类型、ID，以及各路数据的偏移和长度。

读取时会检查目录结构、数据边界和区段重叠。长度为零的区段不包含有效数据，不能仅因其对齐偏移超出文件末尾就判定文件损坏。

**4. 第二步：找出候选装备，让用户确定目标**

工具将模组里的 Unit 与装备快照中的 Unit 引用进行匹配。

例如，模组包含 Unit U，而装备 A、B、C 都引用 U，那么它们都可能进入候选列表。

但工具只能据此知道：

> 这些装备与模组资源存在关联。

它无法仅凭共享关系确定作者实际想替换哪件装备，所以不会默认全选。

用户明确选择后，工具再检查所选装备是否满足完整替换要求，包括相应体型下的非披风 Unit 是否有覆盖。

因此：

- **出现在候选列表中**，表示发现关联。
- **目标检查通过**，表示满足进一步生成的条件。
- **最终生成成功**，还需要依赖、资源及运行时配置全部检查通过。

这也是为什么有些装备能在列表中看到，但选择后仍可能被拒绝生成。

**5. 第三步：追踪实际资源依赖**

确定目标后，工具从目标部件的模型和相关纹理引用出发，沿资源关系继续查找：

```text
目标 Piece
 ├→ Unit
 │   └→ Material
 │       ├→ BaseMaterial
 │       └→ Texture
 └→ Piece 中的动态纹理引用
```

这个过程相当于在资源关系图里搜索，直到没有新的依赖需要处理。

这样可以回答：

- 目标实际需要哪些模型？
- 哪些材质、纹理需要一起隔离？
- 哪些资源由源模组提供？
- 哪些引用仍指向原版游戏资源？

对于单补丁输入，生成器主要输出目标实际使用的依赖集合，不会为了每个 Kit 复制源模组的所有内容。

对于依赖中缺少的材质，工具会尝试读取本机游戏资源核对。Slim 安装通过 `bundles.nxa` 定位资源，不要求游戏目录一定存在散装 Archive 文件。

这里有一个容易遗漏的情况：

```text
私有模型
  → 原版材质
      → 模组修改过的纹理
```

如果原版材质仍引用原纹理 ID，而模组纹理已改成私有 ID，资源关系就断开了。

同样，原版材质的 BaseMaterial 如果指向模组修改的材质，也需要处理。

当前实现会检查这些情况；需要额外克隆外部材质而尚不支持时，会拒绝生成，避免输出表面完整但依赖不成立的包。

**6. 第四步：生成包身份和私有资源 ID**

工具先计算一个包 ID，用来区分不同的输入和目标组合。

单补丁模式的包身份包含：

- 输入三路数据的内容哈希；
- 选中的目标 Kit 集合；
- 生成格式和已知修正规则；
- 显式提供的并存包清单哈希。

这些信息经过 SHA-256，取前 24 个十六进制字符作为包 ID。

因此，包身份取决于输入内容和生成条件，不只是文件名。

随后工具为资源构造带命名空间的名称，大致是：

```text
mods/armor_isolation/v1/<包ID>/kit_<归属>/<资源类型>/<原ID>
```

再通过代码中的 MurmurHash64A 生成 64 位资源 ID。发生冲突时附加盐值，重新计算。

两种哈希用途不同：

| 哈希 | 用途 |
|---|---|
| SHA-256 | 检查文件内容、绑定输入及生成包身份 |
| MurmurHash64A | 根据命名空间名称生成资源 ID |

分配时同时避让完整 64 位 ID 和高 32 位冲突。

不过，这个检查有明确范围：装备快照中的直接引用、源模组、读取过的相关游戏包，以及用户提供的并存包。**它不是扫描所有可能存在的第三方模组后给出的绝对无冲突保证。**

**7. 模型按装备隔离，材质和纹理在包内共享**

目前的资源归属规则是：

- Unit 按目标 Kit 分配私有 ID。
- 同一隔离包内的相同 Material、Texture 复用一组私有 ID。

例如一个包同时替换 A、B：

```text
装备 A → Unit_A ─┐
                ├→ Material_P → Texture_P
装备 B → Unit_B ─┘
```

另一个包使用自己的材质和纹理：

```text
装备 C → Unit_C → Material_Q → Texture_Q
```

这样既能将包与原版、其他隔离包区分开，也避免为多个装备重复存储整套相同纹理。

但两个包如果选择同一个 Kit，仍然冲突，因为同一件装备不能同时指向两套不同配置。插件文件名不同也解决不了这个问题。

**8. 第五步：重写引用，并重建补丁**

给资源换 ID 后，还要修改资源之间的引用。

例如：

```text
原来的模型：引用材质 M
新的模型：引用私有材质 M_P

原来的材质：引用纹理 T
新的材质：引用私有纹理 T_P
```

工具按资源结构解析已知的 64 位引用字段，不会在整份二进制里无差别替换所有相同字节。

这能避免误改：

- 材质和纹理槽的 32 位用途哈希；
- 骨骼索引；
- 网格索引；
- 普通数值字段。

模型数据通常保持原样。已知 B-01 头盔的 LOD 修正是例外，但只有源 Unit 内容哈希与修正规则精确匹配时才应用，不能凭相同资源 ID 泛化到其他模组。

重打包还要重新计算：

1. 类型表和资源数量。
2. 资源目录顺序。
3. 三路文件的磁盘偏移与对齐。
4. 主数据与 GPU 数据的内存缓冲偏移。
5. 头部记录的缓冲大小。

**文件内的位置和加载后的内存缓冲位置是两套信息。** 只改磁盘偏移、沿用旧缓冲字段，仍可能使游戏错误加载资源。

当前内存缓冲布局按资源大小逐项向上对齐 256 字节累计。

写出后，工具重新读取输出目录，核对资源内容与长度，并确认源文件没有变化。

**9. 带配件和分辨率选项的模组如何处理**

模块化模组不能简单地把所有子目录里的补丁同时合并。

例如：

```text
本体
├─ 配件开关
└─ 分辨率
   ├─ 4K
   └─ 8K
```

不同组件可能：

- 提供本体缺少的材质；
- 用相同 Unit ID 覆盖本体模型；
- 使用相同材质 ID，但引用不同纹理；
- 互斥，不能同时启用。

所以工具先理解 manifest 的选择语义，再建立跨组件资源关系。

本体和配件如果修改同一个模型，它们必须使用一致的私有 Unit ID。这样选择配件后，覆盖关系仍能延续。

4K/8K 分支则更特殊：

```text
4K：Material M → Texture T4
8K：Material M → Texture T8
```

如果插件要求 T4 和 T8 同时加载，用户无论选哪个分辨率，都会缺另一份依赖。

因此，在材质结构、纹理槽、独占引用等条件符合要求时，工具将它们归一到同一个逻辑私有纹理 ID：

```text
4K 分支提供：Texture T_P 的 4K 内容
8K 分支提供：Texture T_P 的 8K 内容
```

插件只等待 T_P，由实际选择的分支提供它。

工具同时保留原目录、清单字节和附带文件。为保留整个选项结构而留下的非目标资源，也会私有改名，避免原 ID 补丁继续影响其他装备。

这个过程保留既有选项关系，但不会把清单排列顺序当成已证明的游戏覆盖优先级。

**10. 第六步：生成插件可以理解的配置**

隔离包中的运行时 JSON 声明：

- 预期游戏版本；
- 包 ID；
- 目标 Kit；
- 预期 Body、Piece 布局；
- 原资源与私有资源映射；
- 允许修改的 Piece 资源字段；
- 每个 Kit 实际需要等待的资源。

它不提供任意内存地址，也不包含可执行脚本。

可以把它理解为一份受约束的声明：

> 当指定装备的结构与原引用都符合预期，并且这些私有资源已经就绪时，将允许修改的资源字段切换到对应私有 ID。

生成工具还会调用原生校验器。校验器与插件使用相同的配置加载代码，从而提前检查插件能否接受这些配置。

如果提供了并存包清单，也会把它们一起纳入配置组检查。

**11. 为什么生成失败不会留下一个“看似成功”的结果**

完整生成过程先在自有临时目录中进行：

```text
临时生成补丁
 → 回读检查
 → 导出运行时 JSON
 → 原生配置校验
 → 复制已验证插件和说明
 → 记录文件大小及哈希
 → 发布最终目录
```

只有上述步骤通过，才将完整包移动到最终输出位置。

目标目录已存在时不覆盖；失败时清理本次临时产物。

普通生成过程不写游戏目录，也不修改源模组。生成结果中的 `game_runtime_verified` 仍为 `false`，因为离线检查无法替代游戏实际加载。

**12. 游戏启动后，ReShade 插件怎样工作**

ReShade 为插件提供加载和回调入口。这里的主要工作是游戏数据配置处理，不是屏幕滤镜运算。

插件注册 `present` 回调，并将实际检查节流到约每秒一次，避免每帧重复处理。

首次初始化时，它读取：

```text
ArmorIsolation/*.json
```

配置是整组检查的，包括：

- JSON 格式和重复键；
- 包与目标重复；
- 资源归属；
- 完整 ID 与高 32 位冲突；
- 允许的字段；
- 依赖与数量限制。

任何配置损坏或组内冲突，都会阻止这组配置开始发布。

配置通过后，每个目标 Kit 独立等待自己的资源。不会要求所有包、所有装备的资源同时就绪。

所以这里有两种不同的边界：

| 阶段 | 行为 |
|---|---|
| 配置加载 | 整组验证，不接收冲突配置组 |
| 实际应用 | 每个 Kit 独立检查、独立发布 |

一个 Kit 成功，另一个仍等待，是正常情况。它不是跨所有装备的统一回滚事务。

**13. 插件如何避免把引用写到错误位置**

运行时不只依赖生成时的文件哈希。

插件还会检查加载后的模块特征、关键代码字节，以及目标装备的实际结构。

找到 Kit 后，核对的内容包括：

- Kit ID、所属 Archive、类型和被动；
- Body 数量及体型；
- Piece 数量；
- 原 Unit、Slot、Type、Weight 等；
- 计划修改字段的原资源值。

这些检查主要用于确认“当前看到的就是预期结构”。

随后插件检查该 Kit 的私有资源是否已经由游戏加载。**插件不会主动把补丁资源注入资源管理器；资源仍需要先通过游戏的正常补丁加载流程出现。**

若资源未就绪，就继续等待。

**14. 真正的修改方式：克隆配置，再原子切换指针**

这是运行时实现的关键。

插件先读取目标 Kit、Body、Piece，构造一份独立副本。在副本中修改允许的模型和纹理引用，其余数据保留。

然后分配独立内存，修正副本内部的指针：

```text
新 Kit
 └→ 新 Body 数组
      └→ 新 Piece 数组
```

最后，将装备表中指向旧 Kit 的指针切换到新 Kit：

```text
应用前：
装备表[A] → 原 Kit

应用后：
装备表[A] → 克隆 Kit → 私有资源
```

发布前会再次读取原配置，并重新检查资源就绪状态，防止游戏在处理期间发生重载。

指针切换使用原子比较交换：

> 只有装备表中的指针仍等于刚才读取的旧值，才替换为新值。

如果旧指针已经变化，发布失败，尚未使用的克隆内存会释放，稍后重新检查。

这保证了该次指针替换的原子性，但不等于锁住整个游戏的装备系统。

**15. 为什么 APPLIED 之后可能还要切换一次装备**

插件改变的是装备配置入口。

已经创建好的角色、预览对象或场景模型，可能仍持有之前装配出的对象。配置指针更新不会自动重建所有已存在的外观。

因此日志会提示切换到其他装备，再切回来，让游戏重新装配。

可以分成三个状态理解：

| 状态 | 含义 |
|---|---|
| 私有资源就绪 | 游戏资源系统中已能找到需要的资源 |
| 配置已发布 | 目标 Kit 入口已切换到克隆配置 |
| 外观正确 | 游戏实际使用新配置完成模型、材质和场景装配 |

`APPLIED` 主要证明第二项，不能单独证明第三项。

即使引用正确，源模型自身的 LOD、网格布局、材质或体型适配问题，仍可能使显示异常。

**16. 为什么不能热卸载或自动恢复**

新 Kit 发布后，游戏其他对象可能已经保存了其中的指针。

这时直接释放克隆内存，就可能留下指向无效内存的引用。即使把装备表指针切回原值，也不能证明其他地方不再使用克隆对象。

因此当前设计是：

- 发布失败且未被游戏引用的内存可以释放。
- 已发布内存保留至进程结束。
- 每个 Kit 的发布次数有上限，当前为 16 次。
- 已发布配置发生异常变化时，停止该目标后续写入。
- 修改配置、卸载或恢复原版需要完整重启游戏。

`Enabled=0` 只停止继续发布；`DiagnosticOnly=1` 只检查而不进行新的发布。两者都不会自动撤销已发布配置。

**17. 日志分别说明什么**

| 日志 | 含义 |
|---|---|
| `CONFIG` | 配置组已经通过校验 |
| `EMPTY` | 没有可用配置，需要添加后重启 |
| `WAIT` | 目标、模块或依赖尚未满足条件，或者发布前状态发生变化 |
| `READY` | 诊断模式下检查通过，没有进行配置写入 |
| `APPLIED` | 已发布或确认私有配置仍在使用 |
| `FAILED` | 某项检查失败；是否停止后续处理需结合日志原因判断 |

排查时应按顺序看：**配置是否接受 → 资源是否加载 → 引用是否发布 → 游戏是否重新装配 → 外观是否正确**。这些阶段不能互相替代。

对应实现可从以下代码继续查看：

- [通用资源生成器](G:/Temp/Githud/hd2_mods-test/tools/build_generic_isolated.py)
- [资源引用重写与重打包](G:/Temp/Githud/hd2_mods-test/tools/build_cm14_isolated.py)
- [模块化生成器](G:/Temp/Githud/hd2_mods-test/tools/build_modular_isolated.py)
- [完整生成与发布流程](G:/Temp/Githud/hd2_mods-test/tools/armor_isolation_tool.py)
- [运行时配置校验](G:/Temp/Githud/hd2_mods-test/include/armor_runtime_profile.hpp)
- [游戏端配置克隆与发布](G:/Temp/Githud/hd2_mods-test/src/cm14_isolation_addon.cpp)